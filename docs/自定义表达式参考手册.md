# 自定义表达式参考手册

风场编辑器自定义表达式功能完整指南

---

## 📋 目录

1. [快速开始](#快速开始)
2. [基础表达式](#基础表达式)
3. [波动函数](#波动函数)
4. [径向与圆形函数](#径向与圆形函数)
5. [复杂图案](#复杂图案)
6. [实用技巧](#实用技巧)
7. [参数调整](#参数调整)
8. [常见问题](#常见问题)

---

## 🚀 快速开始

### 输入格式

自定义表达式支持两种格式：

**格式1（推荐）**：包含 `z = `
```
z = sin(x) * cos(y + t)
```

**格式2（简洁）**：不包含 `z = `
```
sin(x) * cos(y + t)
```

### 可用变量

| 变量 | 说明 | 范围 |
|------|------|------|
| `x` | 列坐标（水平） | -20 到 +19（相对于中心）|
| `y` | 行坐标（垂直） | -20 到 +19（相对于中心）|
| `t` | 时间参数 | 0 到 10秒（可配置）|

### 可用函数

**三角函数**：`sin`, `cos`, `tan`, `arcsin`, `arccos`, `arctan`
**双曲函数**：`sinh`, `cosh`, `tanh`
**指数对数**：`exp`, `log`, `log10`, `sqrt`, `power`
**其他**：`abs`, `floor`, `ceil`, `round`

### 常用常数

- `pi` = 3.14159...
- `e` = 2.71828...

---

## 📐 基础表达式

### 1. 线性渐变

#### 水平渐变
```
z = x / 20 * 50 + 50
```
从左到右平滑过渡，范围 0-100%

#### 垂直渐变
```
z = y / 20 * 50 + 50
```
从上到下平滑过渡

#### 对角渐变
```
z = (x + y) / 40 * 50 + 50
```
从左上到右下对角渐变

### 2. 平面

#### 水平平面
```
z = 50
```
所有位置相同的值

#### 斜平面
```
z = (x + y) / 4 + 50
```
沿对角线方向倾斜

---

## 🌊 波动函数

### 1. 简单波

#### 单向波（X方向）
```
z = sin(x / 3) * 50 + 50
```
沿X轴的正弦波

#### 双向波
```
z = sin(x / 3) * cos(y / 3) * 50 + 50
```
X和Y方向的正弦波叠加

#### 旋转波
```
z = sin((x + y) / 4) * 50 + 50
```
45度方向传播的波

### 2. 动态波（带时间参数）

#### 行波
```
z = sin(x / 3 - t) * 50 + 50
```
向右移动的波

#### 驻波
```
z = sin(x / 3) * cos(t) * 50 + 50
```
振幅随时间变化的波

#### 多频波
```
z = (sin(x / 3 - t) + sin(x / 6 - t * 2)) * 25 + 50
```
两个不同频率波的叠加

### 3. 格塞尔波（Gerstner Wave）

```
z = sin(x / 5 - t) * exp(-y*y / 200) * 50 + 50
```
模拟真实水波的衰减效果

---

## 🎯 径向与圆形函数

### 1. 径向波（从中心向外）

#### 基础径向波
```
z = sin(sqrt(x*x + y*y) / 4 - t) * 50 + 50
```
从中心向外扩散的同心圆波

#### 衰减径向波
```
z = sin(sqrt(x*x + y*y) / 4 - t) / (1 + sqrt(x*x + y*y) / 10) * 100
```
距离越远振幅越小

#### 高频径向波
```
z = sin(sqrt(x*x + y*y) / 2 - t * 2) * 50 + 50
```
密集的同心圆纹路

### 2. 环形图案

#### 单环
```
z = exp(-(sqrt(x*x + y*y) - 10)^2 / 20) * 100
```
半径为10的单环

#### 多环
```
z = cos(sqrt(x*x + y*y) / 3) * 50 + 50
```
多个同心圆环

#### 螺旋环
```
z = sin(sqrt(x*x + y*y) / 3 - arctan(y, x) * 3 - t) * 50 + 50
```
螺旋形的波纹

### 3. 中心聚焦

#### 高斯分布
```
z = exp(-(x*x + y*y) / 50) * 100
```
中心高四周低的钟形曲线

#### 宽高斯
```
z = exp(-(x*x + y*y) / 100) * 100
```
更宽的高斯分布

#### 窄高斯
```
z = exp(-(x*x + y*y) / 25) * 100
```
更窄的高斯分布

---

## 🎨 复杂图案

### 1. 几何图案

#### 棋盘格
```
z = (sin(x / 2) * sin(y / 2) + 1) * 50
```
方格图案

#### 条纹
```
z = (sin(x / 3) + 1) * 50
```
垂直条纹
```
z = (sin(y / 3) + 1) * 50
```
水平条纹

#### 斜条纹
```
z = (sin((x + y) / 4) + 1) * 50
```
45度斜条纹

### 2. 干涉图案

#### 双点干涉
```
z = (sin(sqrt((x-5)^2 + y*y) / 3 - t) + sin(sqrt((x+5)^2 + y*y) / 3 - t)) * 25 + 50
```
两个点源产生的干涉条纹

#### 多点干涉
```
z = (sin(sqrt((x-8)^2 + y*y) / 3 - t) + sin(sqrt((x+8)^2 + y*y) / 3 - t) + sin(sqrt(x*x + (y-8)^2) / 3 - t)) * 33 + 50
```
三点干涉

### 3. 特殊曲线

#### 利萨如图形
```
z = (sin(x / 4 + t) * sin(y / 5 + t * 1.3) + 1) * 50
```
复杂的闭合曲线图案

#### 玫瑰曲线
```
z = (cos(5 * arctan(y, x)) * exp(-(x*x + y*y) / 100) + 1) * 50
```
5瓣玫瑰图案

#### 心形
```
z = -(sqrt(x*x + y*y) - 15)^3 - x*x * y*y*y + 50
```
心形图案

---

## ⚙️ 实用技巧

### 1. 坐标变换

#### 偏移中心
```
z = sin((x - 5) / 3 - t) * 50 + 50
```
中心向右偏移5个单位

#### 旋转
```
z = sin((x + y) / 4 - t) * 50 + 50
```
45度旋转

#### 缩放
```
z = sin(x / 6 - t) * 50 + 50
```
波长加倍（x / 6 而不是 x / 3）

### 2. 振幅调制

#### 线性衰减
```
z = sin(x / 3 - t) * (40 - x) / 40 * 50 + 50
```
向右振幅逐渐减小

#### 指数衰减
```
z = sin(x / 3 - t) * exp(-x*x / 100) * 100
```
距离越远振幅衰减越快

#### 包络调制
```
z = sin(x / 3 - t) * exp(-(y*y) / 100) * 100
```
Y方向的高斯包络

### 3. 频率调制

#### 线性调频
```
z = sin(x / 3 + x*x / 100 - t) * 50 + 50
```
频率随位置变化

#### 振荡调频
```
z = sin(x / 3 + sin(x / 10) * 2 - t) * 50 + 50
```
周期性变化的频率

### 4. 组合技巧

#### 多层叠加
```
z = (sin(x / 5 - t) * 0.5 + sin(x / 10 - t * 2) * 0.3 + sin(y / 5 - t) * 0.2) * 100
```
多个波的加权叠加

#### 非线性变换
```
z = (sin(x / 4 - t) + sin(y / 4 - t))^2 / 4 * 100
```
波的平方增强对比度

#### 阈值处理
```
z = (sin(x / 3 - t) > 0) * 100
```
二值化（只有0或100）

---

## 🎛️ 参数调整

### 波长控制

**长波**（图案稀疏）
```
z = sin(x / 10 - t) * 50 + 50
```

**短波**（图案密集）
```
z = sin(x / 2 - t) * 50 + 50
```

**极短波**
```
z = sin(x - t) * 50 + 50
```

### 频率控制

**低频动画**（缓慢变化）
```
z = sin(x / 3 - t * 0.5) * 50 + 50
```

**高频动画**（快速变化）
```
z = sin(x / 3 - t * 3) * 50 + 50
```

### 振幅控制

**小振幅**
```
z = sin(x / 3 - t) * 20 + 50
```
变化范围：30-70%

**大振幅**
```
z = sin(x / 3 - t) * 80 + 50
```
变化范围：-30-130%（会裁剪到0-100）

**满振幅**
```
z = (sin(x / 3 - t) + 1) * 50
```
变化范围：0-100%

### 相位控制

**向右移动**
```
z = sin(x / 3 - t) * 50 + 50
```

**向左移动**
```
z = sin(x / 3 + t) * 50 + 50
```

**旋转移动**
```
z = sin((x + y) / 4 - t) * 50 + 50
```

---

## 💡 常用模板

### 自然风场
```
z = (sin(x / 8 - t * 0.5) * 0.3 + sin(y / 8 - t * 0.3) * 0.3 + sin((x + y) / 10 - t * 0.2) * 0.4) * 50 + 50
```
多种波的柔和组合，模拟自然风

### 涟漪扩散
```
z = sin(sqrt(x*x + y*y) / 3 - t * 2) * exp(-sqrt(x*x + y*y) / 15) * 100
```
像水滴落入水中的涟漪效果

### 扫描波
```
z = sin(x / 3 - t * 2) * exp(-y*y / 50) * 100
```
像雷达扫描的效果

### 螺旋波
```
z = sin(sqrt(x*x + y*y) / 4 - arctan(y, x) * 2 - t) * 50 + 50
```
旋转的螺旋波纹

### 脉冲波
```
z = exp(-((sqrt(x*x + y*y) - t * 5)^2) / 50) * 100
```
从中心向外扩散的脉冲

### 驻波场
```
z = sin(x / 4) * sin(y / 4) * cos(t) * 100
```
二维驻波，固定波节和波腹

---

## 🔧 高级技巧

### 1. 数学函数组合

#### 指数调制
```
z = exp(sin(x / 4 - t) * 2) * 30 / exp(2) * 100
```
用指数函数调制振幅

#### 对数变换
```
z = log(abs(x) + 1) * sin(t) * 20 + 50
```
对数坐标变换

#### 幂函数
```
z = (power(abs(x), 1.5) * sign(x) / 50 * sin(t) + 1) * 50
```
非线性坐标变换

### 2. 条件表达式

#### 条件切换
```
z = (x > 0) * sin(y / 3 - t) * 50 + (x <= 0) * cos(y / 3 - t) * 50
```
左右两侧不同的波形

### 3. 噪声与纹理

#### 简单噪声
```
z = (sin(x / 3) + sin(y / 3) + sin(x / 7 + y / 7)) * 16 + 50
```
伪随机噪声效果

#### 湍流模拟
```
z = (sin(x / 4 - t) + sin(y / 4 - t * 1.3) + sin((x+y) / 5 - t * 0.7)) * 33 + 50
```
模拟湍流的多频叠加

---

## 📊 实用应用场景

### 1. 空气动力学模拟

#### 层流
```
z = x / 20 * 20 + 40
```
简单的线性流

#### 湍流
```
z = (sin(x / 5 - t * 2) * 0.4 + sin(y / 5 - t * 1.7) * 0.3 + sin((x+y) / 6 - t) * 0.3) * 50 + 50
```
复杂的多向流动

### 2. 热扩散模拟

#### 点热源
```
z = exp(-(x*x + y*y) / 30) * 100
```
单点热源

#### 多热源
```
z = (exp(-((x-8)^2 + y*y) / 30) + exp(-((x+8)^2 + y*y) / 30)) * 50
```
两个热源

### 3. 声场模拟

#### 单点声源
```
z = sin(sqrt(x*x + y*y) / 2 - t * 5) / (1 + sqrt(x*x + y*y) / 5) * 100
```
带衰减的点声源

#### 柱面波
```
z = sin(sqrt(x*x + y*y) / 2 - t * 4) * 50 + 50
```
圆柱面波

---

## ⚡ 性能优化建议

### 1. 避免复杂计算

**避免**：
```
z = power(sin(x), 5) + power(cos(y), 5) - t  # 非常慢
```

**推荐**：
```
z = sin(x / 3) * sin(y / 3) - t  # 更快
```

### 2. 使用简化函数

**避免**：
```
z = exp(sin(atan(y, x) * 5)) / log(abs(x) + 1)
```

**推荐**：
```
z = sin(sqrt(x*x + y*y) / 4 - t)
```

### 3. 预计算常量

**避免**：
```
z = sin(x / (3 + sqrt(2))) * cos(y / (5 - pi))
```

**推荐**：
```
z = sin(x / 4.414) * cos(y / 1.858)
```

---

## ❓ 常见问题

### Q1: 表达式报错 "invalid syntax"

**原因**：表达式语法错误

**解决方案**：
- 检查括号是否匹配
- 检查函数名是否正确
- 确保使用的是允许的函数和变量

### Q2: 结果全是 0 或 100

**原因**：表达式超出了范围或计算结果为常数

**解决方案**：
```
# 添加缩放和偏移
z = (your_expression - min_value) / (max_value - min_value) * 100
```

### Q3: 动画不动

**原因**：表达式中没有包含时间参数 `t`

**解决方案**：
```
# 添加时间参数
z = sin(x / 3 - t) * 50 + 50  # 正确
z = sin(x / 3) * 50 + 50      # 错误，没有时间参数
```

### Q4: 图案不对称

**原因**：x 和 y 的处理不一致

**解决方案**：
```
# 确保对称处理
z = sin(x / 3) * sin(y / 3) * 50 + 50  # 对称
z = sin(x / 3) * sin(y / 4) * 50 + 50  # 不对称
```

### Q5: 边缘有突变

**原因**：函数在边界处不连续

**解决方案**：
```
# 使用窗口函数
z = sin(x / 3 - t) * exp(-(x*x + y*y) / 200) * 100
```

---

## 📚 参考公式速查

### 基础波形
| 效果 | 表达式 |
|------|--------|
| 正弦波 | `sin(x/3-t)*50+50` |
| 余弦波 | `cos(x/3-t)*50+50` |
| 交叉波 | `sin(x/3)*cos(y/3)*50+50` |
| 旋转波 | `sin((x+y)/4-t)*50+50` |

### 径向效果
| 效果 | 表达式 |
|------|--------|
| 同心圆 | `sin(sqrt(x²+y²)/4-t)*50+50` |
| 高斯分布 | `exp(-(x²+y²)/50)*100` |
| 环形 | `exp(-(sqrt(x²+y²)-10)²/20)*100` |
| 螺旋 | `sin(sqrt(x²+y²)/4-arctan(y,x)*2-t)*50+50` |

### 组合效果
| 效果 | 表达式 |
|------|--------|
| 行波+衰减 | `sin(x/3-t)*exp(-x²/100)*100` |
| 干涉 | `(sin(r1/3-t)+sin(r2/3-t))*25+50` |
| 驻波 | `sin(x/3)*sin(y/3)*cos(t)*100` |
| 涟漪 | `sin(r/3-t*2)*exp(-r/15)*100` |

---

## 🎓 学习建议

1. **从简单开始**：先尝试基础的正弦波 `sin(x/3-t)*50+50`
2. **逐步增加复杂度**：每次只添加一个修改
3. **观察效果**：应用后查看3D视图，理解参数的作用
4. **调试参数**：逐步调整参数，观察变化
5. **参考模板**：使用本文档中的模板作为起点

---

## 📝 更新日志

- **v1.0** (2024-01-03): 初始版本，包含基础表达式和常用模板

---

**祝你创造出精彩的风场效果！** 🎨✨
